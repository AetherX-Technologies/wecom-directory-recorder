# 本次验证记录

## 自动打开与窗口归位（2026-09-21）

- 新增 `startup.py setup/prepare`、`open-wecom.cmd` 和启动菜单选项 4/5。首次保存本机导航模板，后续查找唯一主窗口，必要时启动已保存的企业微信程序，恢复校准窗口，进入通讯录及全体组织树。
- 本机已用真实窗口图像生成导航模板并完成成功实测，识别 21 个可见树节点。只准备页面，不录屏、不自动回树顶、不自动定位指定部门或登录。
- 实机把窗口从 `(365,146)` 移至 `(265,196)`，尺寸从 `1384×825` 改为 `1284×775`，并切至消息页；程序恢复原位置尺寸并进入通讯录。该次运行经历 Ghost 无响应，随后前台为 `PerryShadowWnd`，按原保护停止，没有报告成功。观察客户端恢复后重新执行，完整导航和验证成功。
- 入口文字选中后颜色变化造成首轮匹配失败；改用紧框的通讯录小图标，真实选中/未选中样本匹配分数约 0.985。没有降低 0.90 匹配阈值，也没有使用固定坐标盲点。
- 共 44 项单元测试通过，包括唯一/重复/缺失模板、布局变动、搜索区域越界、启动后重新查找句柄、登录超时不点击、导航失败不写配置。未为验证而退出正在使用的企业微信；未运行时启动及句柄变化路径由模拟测试覆盖，不能视为真实冷启动验收。
- 导航模板、本机配置备份和原始图像都已加入 Git 忽略规则；原录屏及交付文件没有修改。

当前结论以本文后续实机补充为准，最初“桌面不可用”仅为早期阶段记录。

2026-09-20，Windows 本地目录。

- 已创建 `.venv` 并安装所需三项直接依赖；未修改全局 Python 环境。
- 8 项合成图像测试通过：完整行检测、底部截断排除、空白区域、蓝色选中、箭头匹配、滚动衔接、丢失/重复/反向衔接拒绝。
- Python 源文件编译检查通过，CLI 帮助可运行。
- 用户提供的截图离线检测：26 个完整可见行、1 个收起箭头、8 个展开箭头、1 个蓝色选中行。底部截断行未计入。
- 该裁剪图重复扫描 100 次，每轮检测行及两类箭头，平均约 5.32 毫秒。这是图像算法耗时，不包括桌面截图、鼠标等待、视频编码，不能当作整机 CPU 利用率。
- FFmpeg 用内建白色合成源验证 H.264、4 FPS、ultrafast、CRF 18、两线程编码，退出码 0。未抓取桌面。
- 当前会话原生桌面控制不可用，因此没有运行 `calibrate` 或 `run` 控制企业微信，没有生成真实通讯录视频，也没有验证当前客户端树行空白点击与完整遍历。

证据：`evidence/offline-check.json` 与 `evidence/screenshot-detection.png`。需要用户启动后的短试跑完成实际适配验收。

## 实机验证补充

桌面专用运行时后来成功可用。实机确认部门整行点击会切换展开状态，修复了“先展开再点整行会重新收起”的错误，并补上选中箭头反色匹配；目前 9 项图像算法测试通过。

- `runs/20260920-180952-794637`：16 行，44.75 秒，984×794，H.264 4 FPS；16 张 PNG；审计全部通过。抽取视频帧与对应 PNG，人员姓名/头像/资料对应且清晰。
- `runs/20260920-181125-630562`：40 行，142.75 秒；9 个部门行、31 个其他行，17 次滚动衔接，40 张 PNG；审计全部通过。
- 根部门页面显示 6,404 人，这不是程序已录制的人数，也不直接等于所有部门路径下的人员出现次数。
- 全量录制尚未完成；后续以运行日志和视频核对为准。

续录及边界验证：

- `runs/20260920-181631-278559` 接续 40 行段，新增 35 行；134 秒，35 张 PNG，审计通过。
- `runs/20260920-182017-293931` 再接续 25 行；70.5 秒，25 张 PNG，2 次滚动，审计通过。
- 三段连续链共 100 行，成功合并为 `delivery/20260920-182218/capture-all.mkv`，ffprobe 验证时长 347.25 秒、大小 9,913,882 字节。它仍是部分录制。
- 修复底部只有空白边距被裁掉时误排除最后一行的问题，加入裁剪锚点中心偏移测试；11 项算法测试通过。此修复将从下一录制段生效。
- 录制中一次资源采样：Python 主进程工作集约 47.5 MB，FFmpeg 约 51.3 MB（不含系统和企业微信）；这是瞬时样本，不是长期峰值或 CPU 使用率结论。
- 自动分段主管的停止、编码失败、审计失败和末尾候选四条边界用模拟进程测试；共 15 项测试通过。主管已启动，等待当前 500 行段结束后继续。
- `evidence/performance-sample.json` 保存本段约 12 分钟时的进程采样：FFmpeg 45.32 MB、Python 59.13 MB；平均 CPU 分别为单个逻辑核心的 0.314% 和 0.220%。不包含企业微信和系统，也不表示峰值。
- 审计新增逐张 PNG 的目标行选中验证、相邻人员右侧详情完全不变的提示；40 行段和 35 行段再次审计均通过，未发现需人工核对的相邻详情不变对。

## 18:48 恢复验证

- 359 行段 `20260920-182220-800865` 的全部 PNG、日志及视频审计通过；因长名称白字干扰选中状态检测而停止，已修复为上下边缘宽蓝色带检测。
- 后续 1 行段和 11 行段产物审计均通过。桌面控制工具的带光圈指针叠层落在树内，导致邻行粘连；已移动到树外，严格续录像素对比通过。
- 行检测新增蓝色选中带独立分组，防止与相邻图标合并；17 项测试通过。当前正在继续短试跑及分段录制。
- 上述连续链累计 471 行，不等于独立人数；完整覆盖仍未验证。

## 持续录制中的加强审计

- 修复后的 20 行段 `20260920-184819-092802` 完成，续录主管已开始 `20260920-184922-489316`。
- 7 个已结束的连续段共 491 行，重新审计全部通过；除选中状态外，新增逐个部门的展开箭头验证、人员候选误含文件夹检查，未发现异常。汇总见 `evidence/chain-audit-1857.json`。
- 新段会保存实际箭头和文件夹模板，审计记录模板 SHA-256。早期段使用当前校准模板回查，报告明确注明模板来源，不能声称早期段已保存当时模板。
- 当前段尚在录制，未将其计入上述已结束段的完整审计。

## 19:13 首个完整 500 行段

- `20260920-184922-489316` 已正常录满 500 行：66 个部门、434 个人员候选、36 次滚动衔接；全部 500 张 PNG 选中状态与部门展开状态审计通过，无相邻人员详情完全不变提示。
- 视频 H.264、984×794、4 FPS、1392.5 秒、31,071,937 字节。抽取中间第 250 行的编码视频帧 `evidence/segment500-midframe.png`，确认左右人物一致、文字清晰。
- 已结束并审计的连续链共 991 行。主管已自动接续 `20260920-191244-862074`，该段保存了识别模板。全量仍未完成。

## 22:10 界面更新延迟恢复

- 至 `20260920-213207-995407` 的已结束连续链共 4491 行，逐段审计均通过。
- `20260920-215522-391769` 完成 209 行后，下一部门点击未在等待期内更新；末帧仍是第 209 人，后续原生观察显示下一部门已选中。根因未完全确定，已增加最长 10 秒的选中等待（不重复点击），延迟确认后重新停留，并保存失败画面。
- 此段视频 580.5 秒，最后确认事件 580.308 秒，程序结束 585.33 秒；全运行时长匹配检查失败，其余检查通过。已人工对照视频末帧和第 209 张 PNG，确认最后已记录的人物和资料一致；未记录尾部属于其后的未确认点击/退出阶段。
- 保留原始审计失败，`tail-review.json` 以 SHA-256 绑定视频、事件、总结、尾帧和末张 PNG。合并仅接受这一种已复核的尾部例外，并在 manifest 保留完整记录；其他失败或文件变更拒绝通过。
- 恢复原树画面后，严格像素对比通过；20 行续录 `20260920-220902-126840` 全部审计通过，接续段 `20260920-221047-043349` 已运行。连续链至该 20 行段为 4720 行，仍非全量完成。

## 22:51 头像延迟加载导致的滚动恢复

- `20260920-221047-043349` 的 500 行、`20260920-223401-303475` 的 176 行产物审计全部通过。后者滚动后头像从默认图变为照片，整行相关系数降低而停止，最后成功节点后的滚动尚未记为成功。
- 人员行新增明确启用的选中姓名笔画匹配，仍要求唯一匹配、蓝色选中和前进方向；部门保持原有整行匹配。恢复异常滚动还必须同时匹配上一行上下文，不能仅凭姓名定位。
- `--resume-after-scroll` 恢复段 `20260920-225005-956459` 完成 20 行（10 部门、10 人员候选、1 次滚动），所有产物检查通过。连续链累计 5416 行，仍非唯一人数和全量证明。
- 新增头像变化、其他姓名、重复姓名和反向滚动验证；共 24 项测试通过。此前视频尾部复核例外继续保留。

## 23:18 恢复后的完整分段

- `20260920-225147-045338` 完成 500 行：216 个部门、284 个人员候选、48 次滚动；全部 500 张 PNG、部门展开状态和视频事件审计通过，无相邻人员详情完全不变提示。
- 视频时长 1607 秒，H.264、984×794、4 FPS，28,112,149 字节。抽查第 87、256 行人员画面姓名与选择一致，第 321 行为空部门，展开状态正确。
- 已结束连续链累计 5916 行，包含重复人员及部门，不能作唯一人数。下一段 `20260920-231844-477822` 已自动开始；完整树末尾仍未核实，先前退出尾部例外仍保留。

## 23:48 后续分段审计

- `20260920-231844-477822` 完成 500 行：73 个部门、427 个人员候选、72 次滚动；全部产物检查通过，无部门状态异常或相邻人员详情完全不变提示。
- 视频 1770.75 秒、27,768,592 字节，H.264、984×794、4 FPS。抽查第 59 和 280 行，选中姓名与右侧资料一致、文字清晰。
- 已结束连续链累计 6416 行，下一段 `20260920-234825-901656` 已自动开始；仍未核实完整树末尾，原尾部复核例外继续保留。

## 9 月 21 日 00:24 分段审计

- `20260920-234825-901656` 完成 500 行：202 个部门、298 个人员候选、60 次滚动，全部产物检查通过。
- 视频 2108.25 秒、33,930,818 字节，H.264、984×794、4 FPS。抽查第 52 行部门展开正常，第 275 行人员选择与资料对应。
- 已结束连续链累计 6916 行；下一段 `20260921-002344-650578` 已启动。完整树末尾仍未核实，原尾部复核例外继续保留。

## 9 月 21 日 00:34 失焦停止与恢复

- `20260921-002344-650578` 在 87 行后检测企业微信失去前台焦点而停止；87 行产物全部审计通过，视频 422.25 秒，无新增审计例外。失焦来源尚未确定。
- 原生观察确认树仍停在最后记录人员；恢复焦点后，正常续录的严格树像素对比通过，没有放宽校验。
- `20260921-003139-878203` 短段 20 行（8 部门、12 人员候选、2 次滚动）全部审计通过，视频 104.5 秒。已结束连续链累计 7023 行。
- 自动分段已从 `20260921-003358-599597` 继续；仍未核实完整树末尾，原尾部复核例外继续保留。

## 9 月 21 日 00:54 滚动动画稳定检查

- `20260921-003358-599597` 完成 204 行后，在滚动后因相邻行间距过大停止；204 行产物审计全部通过，视频 900.5 秒。
- 日志滚动定位为 y=184，停止后的稳定画面为 y=169，位置仍变化造成下一行距离判断异常。新增滚动后至少两秒观察、连续三次图像稳定才继续的检查，持续变化则停止；含延迟移动、持续移动、未移动最短等待的 27 项测试通过。
- `--resume-after-scroll` 扩展至此类间距停止，仍核对原选中行和前一行上下文。恢复记录从 y=725 定位到 y=169，前一行相关系数 0.999923。
- `20260921-005150-265756` 20 行（5 部门、15 人员候选、4 次滚动）全部产物审计通过，视频 102.5 秒。已结束连续链累计 7247 行。
- 自动分段从 `20260921-005353-218748` 继续，仍未核实完整树末尾，原尾部复核例外继续保留。

## 9 月 21 日 01:32 稳定检查后的完整分段

- `20260921-005353-218748` 完成 500 行：123 个部门、377 个人员候选、65 次滚动；全部产物审计通过，没有部门状态异常或相邻人员详情完全不变提示。
- 视频 2292.25 秒、32,917,805 字节，H.264、984×794、4 FPS。抽查第 261 行，人员选择与右侧资料对应、文字清晰。
- 已结束连续链累计 7747 行，自动开始 `20260921-013216-387855`；完整树末尾仍未核实，原尾部复核例外继续保留。

## 9 月 21 日 01:44 失焦与延后滚动恢复

- `20260921-013216-387855` 完成 50 行后失焦停止，产物全部审计通过（207.75 秒）。界面已选中未确认的下一人；恢复最后成功人员后，严格树画面对比通过，再次录制该下一人。
- 恢复段 `20260921-013742-876155` 完成 17 行后选中验证失败，17 行产物全部审计通过（92.75 秒）。滚动记录仅移动约 65 像素，随后失败截图已进一步移动，下一人已选中但不在预期坐标；原两秒稳定观察不足，尚未确定界面延后响应的系统原因。
- 稳定检查改为额外至少五秒且连续两秒图像稳定，27 项测试通过。恢复标志允许选中失败后的双图像核对，但须先恢复最后已确认行；不把失败点击记为完成。
- `20260921-014156-841278` 20 行、2 次滚动全部审计通过（97.25 秒），已结束连续链累计 7834 行，自动开始 `20260921-014400-760635`。原尾部复核例外保留，完整树末尾仍未核实。

## 9 月 21 日 01:58 应用无响应诊断与续录

- `20260921-014400-760635` 60 行、`20260921-015103-745340` 3 行、`20260921-015242-509355` 14 行、`20260921-015458-333137` 3 行均因失焦停止，各段产物审计全部通过。连续链累计 7914 行，尚未完成全树录制。
- 增加失焦窗口句柄及窗口类诊断，两次捕捉到 `Ghost`，符合 Windows 应用无响应替代窗口表现；未确认卡顿根因。保留失焦立即停止保护。
- 失焦也允许显式双图像续录核验，仍须原选中行与上一行匹配。015242 段后界面发生延后滚动，核验成功后续录；015458 段后未确认的下一部门已展开，先恢复折叠和最后成功人员，再严格对图续录。
- 新短段将详情停留延长到 5 秒以观察卡顿影响。27 项测试通过（增加失焦原因恢复分类前），后续仍需完整最终核验。原视频尾部复核例外持续保留。

## 9 月 21 日 02:02 有界无响应等待

- `20260921-015658-095263` 12 行后再次出现 Ghost，全部产物审计通过（79.25 秒）；连续链累计 7926 行。
- 单纯延长 dwell 至 5 秒未消除应用无响应。输入保护改为仅在前台窗口类为 Ghost 时无输入等待至多 30 秒；原目标窗口恢复前台才允许继续，其他前台窗口或超时仍停止，窗口几何和点击遮挡检查保持生效。
- 等待期间支持 Esc、STOP、左上角停止和 F8 暂停；卡顿时间不计入详情 dwell。新增恢复、超时、其他窗口和 Esc 测试，31 项测试通过。真实续录验证进行中。
- `20260921-020027-005054` 完成 20 行（18 人员候选、2 部门、2 次滚动），全部产物审计通过，视频 129.25 秒。此段未捕捉到 Ghost 等待触发，真实等待恢复路径仍需后续观察；不以单元测试代替该实机证据。
- 当前配置 dwell 改为 5 秒，已恢复自动分段至 `20260921-020308-546761`。连续链已完成累计 7946 行，完整覆盖尚未确认。
- `20260921-020308-546761` 6 行后 Ghost 等待恢复，但下一行选中核验未通过，保护停止；产物全部审计通过（52.5 秒），未计入失败点击。原最后人员仍选中，严格对图恢复。
- `20260921-020443-941625` 20 行（16 人员候选、4 部门、2 次滚动）全部产物审计通过（140.25 秒）。运行输出三次 Ghost 等待后继续完成节点，获得真实恢复路径证据。
- 自动分段已接续 `20260921-020726-540916`，已完成连续链累计 7972 行，最终树底核验及完整交付仍未完成。

## 9 月 21 日 02:22 滚动越过衔接行后的恢复

- `20260921-020726-540916` 完成 104 行（83 人员候选、21 部门、16 次已核验滚动），全部产物审计通过，视频 756.5 秒。随后滚动中报姓名锚点不匹配，画面已越过原选中人员；未把该次滚动计作成功。
- 原生界面向上滚动找回原选中人员，姓名和工号与末帧一致。允许此明确锚点错误进入双图像恢复流程，最后行与上一行核验成功后开始 `20260921-022133-747877`。31 项测试通过。
- 后续配置 scroll_notches 从 6 改为 3，降低卡顿期间滚动过量风险；当前短段已启动仍使用原配置。连续链已结束累计 8076 行，尚未完成。
- `20260921-022133-747877` 完成 20 行、4 次滚动，全部产物审计通过（140.5 秒）。恢复上下文匹配分数 0.997829。已结束累计 8096 行，自动分段 `20260921-022412-497709` 使用 3 格滚动继续。

## 9 月 21 日 03:35 三格滚动完整分段

- `20260921-022412-497709` 完成 500 行（389 人员候选、111 部门、143 次滚动），全部产物审计通过；视频 4228.5 秒、48,873,536 字节，H.264、984×794、4 FPS。
- 期间多次 Ghost 等待后恢复，无新的锚点或选中失败中断。连续链已结束累计 8596 行，包含重复人员出现及部门行，不是独立人数。
- 自动分段已开始 `20260921-033451-526633`，完整树末尾及最终交付尚未完成；既有视频尾部复核例外继续保留。

## 9 月 21 日 04:43 连续分段完成

- `20260921-033451-526633` 完成 500 行（393 人员候选、107 部门、101 次滚动），全部产物审计通过；视频 4059.75 秒、59,068,129 字节，H.264、984×794、4 FPS。
- 无部门状态异常、无相邻人员详情完全不变提示，期间多次 Ghost 等待恢复后继续。已结束连续链累计 9096 行，自动开始 `20260921-044241-901040`。
- 全树末尾及最终交付仍未完成；原视频尾部复核例外保留。

## 9 月 21 日最终树底确认与交付

- `20260921-044241-901040` 完成 76 行（54 次人员出现、22 次部门出现、20 次滚动），原始产物审计全部通过，视频 731.25 秒。随后误将人数页脚作为下一行点击而停止；失败点击没有计入完成行数。
- 核对末帧和原生界面，最后成功人员下方仅有“共6547人”页脚。增加可选精确页脚模板，仅在底部一个行距、水平居中且匹配分数至少 0.98 时排除未选中页脚。新增匹配、不匹配及选中人员保留测试，合计 34 项测试通过。
- `20260921-045751-461137` 从最后成功行续录，新增 0 行，连续三次向下滚动未移动，返回 `end_candidate`。独立原生观察确认实际树底；编码视频画面另存 `evidence/tree-bottom-confirmed.png`。这个零新增行确认段独立保存，不加入逐行录制链。
- 最终 `delivery/20260921-050012/` 含合并视频、逐行索引、manifest、交付说明及 verification。44 段共 9172 行：7373 次人员出现、1799 次部门出现，1006 次已核验滚动。43 段原始审计通过，1 段保留先前哈希绑定的视频退出尾部复核例外，未改写原始失败。
- 合并视频时长 35579.75 秒（9:52:59.75），614136908 字节，H.264、984×794、4 FPS。全视频解码退出码 0，`evidence/final-decode.log` 为空；总时长等于分段之和，索引连续、时间递增，全部索引 PNG 存在，末条记录位于视频内。实际编码视频开头、中间、末尾已目视抽查。
- 最终文件和末尾证据 SHA256 保存在 `verification.json`。manifest 的末段 `blocked` 和 `coverage_verified: false` 保留历史原值，实际树底确认由独立验证字段说明。
- 本次完成的是当前可访问组织树从顶部至观察到的实际底部的浏览录屏。出现次数不是独立人数，页脚人数也不是去重核验；没有企业接口级完整性证明，没有展开默认隐藏的“点击查看”资料，没有执行后续 OCR。原始分段、截图及失败记录均保留。
